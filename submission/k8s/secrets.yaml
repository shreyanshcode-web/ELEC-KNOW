# IMPORTANT: This is a TEMPLATE. In production, use Google Secret Manager
# with External Secrets Operator or Workload Identity to inject secrets.
# Never commit real secret values to version control.
apiVersion: v1
kind: Secret
metadata:
  name: election-secrets
  namespace: election-education
  labels:
    app.kubernetes.io/name: election-education
type: Opaque
stringData:
  # Replace these placeholders before applying.
  # In production, use `gcloud secrets` or External Secrets Operator.
  DB_USER: "REPLACE_WITH_CLOUD_SQL_USER"
  DB_PASSWORD: "REPLACE_WITH_CLOUD_SQL_PASSWORD"
  GEMINI_API_KEY: "REPLACE_WITH_GEMINI_KEY"
  REDIS_PASSWORD: ""
